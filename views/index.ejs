<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="./css/style.css">
  <link rel="stylesheet" href="./css/common.css">
  <title>AI Assistant</title>


</head>
<body class="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-zinc-900 text-slate-100 antialiased selection:bg-indigo-500/30">

  <div class="mx-auto p-4 md:p-4">
    <!-- Card -->
    <div class="">

      <!-- Header -->
      <header class="flex items-center justify-between gap-3 p-4 md:p-5 border-b border-white/10">
        <div class="flex items-center gap-3">
          <div class="grid size-10 place-items-center rounded-2xl bg-white/10">
            <!-- robot-ish icon -->
            <svg viewBox="0 0 24 24" class="size-5 text-slate-100">
              <path fill="currentColor" d="M11 2h2v3h-2zM7 6h10a3 3 0 0 1 3 3v6a5 5 0 0 1-5 5H9a5 5 0 0 1-5-5V9a3 3 0 0 1 3-3zm-1 9a3 3 0 0 0 3 3h6a3 3 0 0 0 3-3v-3H6zm3.5 2.5a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3m5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3" />
            </svg>
          </div>
          <div>
            <h1 class="text-base font-semibold leading-tight">AI Assistant</h1>
            <p class="text-xs text-slate-400">Answers from your embedded data</p>
          </div>
        </div>

        <!-- Model switch (desktop) -->
        <div class="switchmodel hidden sm:flex items-center gap-2">
          <label for="model" class="text-xs text-slate-400">Model</label>
          <select id="model" name="model"
                  class="rounded-lg bg-white/10 text-sm px-3 py-2 border border-white/10 focus:outline-none focus:ring-2 focus:ring-indigo-500">
            <option value="modelA">Model A</option>
            <option value="modelB">Model B</option>
          </select>
        </div>
      </header>

      <!-- Messages -->
      <main id="messages"
            class="h-[70vh] md:h-[65vh] overflow-y-auto px-4 md:px-5 py-4 space-y-4 scroll-smooth"
            aria-live="polite">
        <!-- Example user message -->
        <div class="flex justify-end">
          <div id="userMessageDisplay">
          
            </p>
          </div>
        </div>

        <!-- Example assistant message -->
        <div class="flex justify-start">
    
        </div>
      </main>

      <!-- Composer -->
      <footer class="p-4 md:p-5 border-t border-white/10">
        <div class="relative">
          <!-- Model switch (mobile, tucked in the corner) -->
          <div class="absolute right-24 top-2 sm:hidden">
            <label for="model-sm" class="sr-only">Model</label>
            <select id="model-sm" name="model-sm"
                    class="rounded-md bg-white/10 text-xs px-2 py-1 border border-white/10 focus:outline-none focus:ring-2 focus:ring-indigo-500">
              <option value="modelA">Model A</option>
              <option value="modelB">Model B</option>
            </select>
          </div>

   
            <form id="messageForm" action="post-message" method="post" me class="flex items-end gap-2">
              <label for="prompt" class="sr-only">Your message</label>
              <textarea name="text" id="prompt" rows="1" placeholder="Ask anything... Shift + Enter for newline"
                        class="flex-1 resize-none rounded-2xl border h-auto border-white/10 bg-white/10 px-4 py-3 pr-28 sm:pr-24 text-sm placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 max-h-60"></textarea>
  
              <input type="submit" value="Send" id="send" class="shrink-0 rounded-2xl border border-indigo-500/30 bg-indigo-600 px-4 py-3 text-sm font-medium text-white hover:bg-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 active:scale-[.99]">
        
 

            </form>
       
      

          <div class="mt-2 flex items-center justify-between text-[11px] text-slate-400">
            <div class="switchmodel hidden sm:flex items-center gap-2">
              <kbd class="rounded border border-white/20 bg-white/10 px-1.5 py-0.5">Shift</kbd>
              <span>+</span>
              <kbd class="rounded border border-white/20 bg-white/10 px-1.5 py-0.5">Enter</kbd>
              <span>for new line</span>
            </div>
            <div class="sm:hidden">Model switch is in the corner</div>
          </div>
        </div>
      </footer>

    </div>
  </div>

  <script src="/js/script.js"></script>
  
<!-- in your HTML layout, not inside your JS file -->
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/dompurify@3.1.7/dist/purify.min.js"></script>
<script type="module">
  import DOMPurify from "https://cdn.jsdelivr.net/npm/dompurify@3.1.7/dist/purify.es.mjs";
  const safe = DOMPurify.sanitize("<p>hola</p>");
  // your code that uses safe
</script>

</body>
</html>
